#!/bin/bash
set -euo pipefail
sudo tee /etc/prometheus/prometheus.yml >/dev/null <<'PROMCONF'
${PROMETHEUS_YML}
PROMCONF
sudo systemctl reload-or-restart prometheus
echo "prometheus reloaded with final loadtest target"
